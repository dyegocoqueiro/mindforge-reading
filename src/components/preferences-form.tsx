"use client";
import { useState } from "react";
import { createSupabaseBrowserClient } from "../lib/supabase/client";
export function PreferencesForm(){const[message,setMessage]=useState("");async function save(data:FormData){const client=createSupabaseBrowserClient();if(!client)return setMessage("Configure o Supabase para salvar preferências.");const {data:{user}}=await client.auth.getUser();if(!user)return setMessage("Entre para alterar suas preferências.");const {error}=await client.from("user_preferences").upsert({user_id:user.id,available_minutes:Number(data.get("minutes")),font_size:Number(data.get("fontSize")),theme:data.get("theme"),reminders_enabled:data.get("reminders")==="on"});setMessage(error?error.message:"Preferências salvas.");}return <form className="settings-form" action={save}><label>Duração padrão<select name="minutes" defaultValue="20"><option value="10">10 minutos</option><option value="15">15 minutos</option><option value="20">20 minutos</option><option value="30">30 minutos</option></select></label><label>Tamanho da fonte<input name="fontSize" type="number" min="16" max="28" defaultValue="18" /></label><label>Tema<select name="theme" defaultValue="system"><option value="system">Do sistema</option><option value="light">Claro</option><option value="dark">Escuro</option></select></label><label className="check-row"><input name="reminders" type="checkbox" /> Ativar lembretes internos</label>{message&&<p role="status">{message}</p>}<button className="button" type="submit">Salvar preferências</button></form>}
