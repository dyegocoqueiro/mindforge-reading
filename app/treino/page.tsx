import { AppShell } from "../../src/components/app-shell";
import { createSupabaseServerClient } from "../../src/lib/supabase/server";
export default async function TrainingPage(){const supabase=await createSupabaseServerClient();const {data:{user}}=supabase?await supabase.auth.getUser():{data:{user:null}};const {data:day}=user?await supabase!.from("training_plan_days").select("id,day_number,total_minutes,blocks,training_plans!inner(user_id)").eq("training_plans.user_id",user.id).eq("status","planned").order("day_number").limit(1).maybeSingle():{data:null};return <AppShell userEmail={user?.email}><header className="page-head"><p className="eyebrow">Sessão adaptada</p><h1>Treino de hoje</h1><p>Preparação, fluência, leitura, compreensão, memória e feedback.</p></header>{day?<section className="training-blocks"><h2>Sessão {day.day_number} · {day.total_minutes} minutos</h2><pre>{JSON.stringify(day.blocks,null,2)}</pre></section>:<section className="empty-inline"><h2>Nenhuma sessão planejada</h2><p>Conclua uma avaliação válida para o motor gerar um plano persistente.</p></section>}</AppShell>}
