import { AppShell } from "../../src/components/app-shell";
import { PracticeLab } from "../../src/components/practice-lab";
import { createSupabaseServerClient } from "../../src/lib/supabase/server";
export default async function PracticePage(){const supabase=await createSupabaseServerClient();const{data:{user}}=supabase?await supabase.auth.getUser():{data:{user:null}};const{data:texts}=user?await supabase!.from("passage_versions").select("id,title,body,passages!inner(status)").eq("passages.status","published").limit(30):{data:[]};return <AppShell userEmail={user?.email}><header className="page-head"><p className="eyebrow">Laboratório de leitura</p><h1>Prática guiada</h1><p>Treine por conta própria com uma coleção variada de textos, ritmo ajustável e resultados persistentes.</p></header>{user?<PracticeLab texts={texts??[]}/>:<section className="empty-inline"><h2>Entre para praticar</h2><p>As tentativas ajustam seu perfil sem armazenar o texto privado das suas anotações.</p><a className="button button-accent" href="/entrar?next=/praticar">Entrar</a></section>}</AppShell>}
